@model WebMvc5.Models.Home.MonthViewModel

@{
    ViewBag.Title = "Month by AP";
}

<h2>Month charts by AP</h2>

@foreach (var chart in Model.Charts)
{
    <div class="row">
        @chart
    </div>
}